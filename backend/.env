# ────────────────────────────────────────────────
#           groundwater-backend - Production Ready
# ────────────────────────────────────────────────

# Server & Environment
NODE_ENV=development                # change to "production" in prod
PORT=5000                           # change if needed

# MongoDB Atlas (your real connection string - NEVER commit this file!)
# For local dev: use mongodb://localhost:27017/groundwater
MONGODB_URI=mongodb+srv://satishvadlamuri47_db_user:u6uBW1z2AQhAmJQq@cluster0.uteykyo.mongodb.net/?appName=Cluster0
# Redis (your real Cloud Redis Labs URL)
# For local dev: redis://localhost:6379
REDIS_URL=redis://default:5y3xPQ0sYLPKYfQE2QLVo914TSnu6Vqp@redis-11186.c273.us-east-1-2.ec2.cloud.redislabs.com:11186

# Logging
LOG_LEVEL=info                      # options: error, warn, info, http, debug

# Rate Limiting (adjust in production)
RATE_LIMIT_WINDOW_MS=900000         # 15 minutes = 900000 ms
RATE_LIMIT_MAX=100                  # max 100 requests per window

# JWT Authentication (change this to a strong random string!)
JWT_SECRET=your_super_long_random_secret_here_at_least_32_chars_change_this_now_2026
JWT_EXPIRY=1h                       # token expiry (1h, 7d, 30d etc.)

# Optional - add if you want to protect routes or add more features later
# ADMIN_EMAIL=admin@example.com
# CORS_ORIGIN=https://your-frontend-domain.com

# Debugging / Development flags
MONGODB_DEBUG=false                 # set true only for debugging queries

